name: Create Tag on Master Push

on:
  push:
    branches:
      - master

jobs:
  create_tag:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Create Tag
        run: |
          git config --local user.email "nrvrangapariya186@gmail.com"
          git config --local user.name "Nirav Rangapariya"
          git tag -a v1.0.7 -m "Auto-generated tag by GitHub Action"
          git push origin --tags

        env:
          GITHUB_TOKEN: "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"
